import React from "react";
import Sidebar from "./components/Sidebar";

const Profile = () => {
  return (
    <div className="flex">
      {/* Sidebar */}
      <Sidebar />
    </div>
  );
};

export default Profile;
